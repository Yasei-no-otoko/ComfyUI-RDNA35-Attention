[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rdna35-pisa-ck"
version = "0.7.0"
description = "GFX1151 CK Tile and FlexAttention PISA forward attention for PyTorch ROCm"
requires-python = ">=3.10"
